# Verified 2026-09-14 (Python 3.10+)
replicate>=1.0.7,<2.0      # official Replicate client (FileOutput, predictions.create, wait=)
Pillow>=10.0
numpy>=1.26
httpx>=0.27                # pulled in by replicate; pinned for the download client
