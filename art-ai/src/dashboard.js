export const dashboard = String.raw`<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>The Dark Matters · Creative Canvas</title>
<style>
:root{--bg:#090907;--panel:#151411;--line:#4a4136;--ink:#f2eee6;--muted:#b7afa3;--cyan:#d5b17a;--gold:#d5b17a;--bad:#d67d70;--green:#a7c89d}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 18% 0,#302820 0,#090907 46rem);color:var(--ink);font:15px system-ui,sans-serif}main{max-width:1280px;margin:auto;padding:32px 22px}h1{font-size:clamp(28px,5vw,52px);margin:7px 0}h2{font-size:19px;margin:0 0 15px}.eyebrow{color:var(--cyan);letter-spacing:.12em;font-weight:700}.sub{color:var(--muted);max-width:770px}.grid{display:grid;gap:14px}.metrics{grid-template-columns:repeat(5,minmax(110px,1fr));margin:25px 0}.two{grid-template-columns:1fr 1fr;margin-top:18px}.card{background:linear-gradient(145deg,#1a1814,#11100e);border:1px solid var(--line);border-radius:15px;padding:18px}.metric b{font-size:32px;color:var(--cyan);display:block}.metric small{color:var(--muted)}.stage-list{display:grid;gap:8px}.stage{display:flex;gap:12px;align-items:center;padding:10px;border:1px solid #3b352d;border-radius:9px}.num{color:var(--cyan);font-weight:800;width:28px}.stage p{margin:2px 0 0;color:var(--muted);font-size:13px}.state{margin-left:auto;color:var(--gold);font-size:12px;text-transform:uppercase}label{display:block;color:var(--muted);font-size:13px;margin:10px 0 5px}input,textarea,select,button{font:inherit}input,textarea,select{width:100%;background:#0c0b09;color:var(--ink);border:1px solid #51483c;border-radius:8px;padding:10px}textarea{min-height:112px}button,.button{border:0;border-radius:8px;padding:10px 13px;background:var(--cyan);color:#241d15;font-weight:750;cursor:pointer;text-decoration:none;display:inline-block}.secondary{background:transparent;color:var(--cyan);border:1px solid var(--cyan)}.candidate,.job,.product{border-top:1px solid #3b352d;padding:14px 0}.candidate:first-child,.job:first-child,.product:first-child{border-top:0}.actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:9px}.muted{color:var(--muted)}.error{color:var(--bad)}.ok{color:var(--green)}.products{max-height:580px;overflow:auto}.product-title{font-weight:700}.product-meta{font-size:13px;color:var(--muted);margin:4px 0}.hidden{display:none}.approval-studio{margin:24px 0;background:linear-gradient(130deg,#201b15,#11100e)}.approval-head{display:flex;justify-content:space-between;align-items:end;gap:20px;margin-bottom:16px}.approval-head p{margin:3px 0 0;color:var(--muted)}.approval-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}.approval-card{border:1px solid #51483c;background:#0e0d0b;border-radius:12px;overflow:hidden}.approval-images{display:grid;grid-template-columns:repeat(3,1fr);gap:2px;background:#29241d}.approval-image{position:relative;aspect-ratio:1/1.2;background:#211d17}.approval-image img{width:100%;height:100%;object-fit:cover;display:block}.approval-image input{position:absolute;top:8px;left:8px;width:18px;height:18px;accent-color:var(--cyan)}.approval-body{padding:15px}.approval-body h3{font-size:17px;margin:2px 0 4px}.approval-tag{font-size:11px;letter-spacing:.12em;color:var(--cyan);text-transform:uppercase}.mode-options{display:flex;gap:6px;flex-wrap:wrap;margin:12px 0}.mode{border:1px solid #51483c;border-radius:999px;padding:6px 8px;color:var(--muted);font-size:12px;cursor:pointer}.mode input{width:auto;margin:0 4px 0 0;accent-color:var(--cyan)}.approval-status{font-size:12px;margin:10px 0 0;color:var(--muted)}.approval-status.approved{color:var(--green)}.approval-status.rejected{color:var(--bad)}@media(max-width:800px){.metrics,.two{grid-template-columns:1fr 1fr}.two{display:block}.two>.card{margin-top:16px}.approval-grid{grid-template-columns:1fr}}@media(max-width:480px){.metrics{grid-template-columns:1fr 1fr}.approval-head{display:block}}
.recipe{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:12px 0}.recipe-item{padding:9px;background:#17140f;border:1px solid #3b352d;border-radius:8px;font-size:13px;color:var(--ink)}.recipe-item b{display:block;text-transform:uppercase;letter-spacing:.08em;font-size:10px;color:var(--cyan);margin-bottom:4px}.auto-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:14px}.auto-item{display:flex;justify-content:space-between;align-items:center;gap:10px;padding:10px;border:1px solid #3b352d;border-radius:8px;background:#17140f;font-size:13px}.auto-item button{padding:6px 8px;font-size:12px}@media(max-width:480px){.recipe,.auto-list{grid-template-columns:1fr}}
.fine-agent{margin:24px 0;background:linear-gradient(145deg,#201a12,#100f0d)}.fine-agent-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px}.range-line{display:flex;justify-content:space-between;gap:12px;align-items:center}.range-line input{padding:0;border:0}.prompt-preview{white-space:pre-wrap;word-break:break-word;min-height:220px;background:#0c0b09;border:1px dashed #51483c;border-radius:10px;padding:14px;color:var(--cyan);font:12px ui-monospace,monospace}@media(max-width:800px){.fine-agent-grid{grid-template-columns:1fr}}
</style></head><body><main>
<div class="eyebrow">THE DARK MATTERS · CREATIVE STUDIO</div><h1>Approve the collection.</h1><p class="sub">Choose the images you love, decide how to sell them, and approve them when they are ready. The system keeps the rest organised in the background.</p><p id="mode" class="ok">Loading production status…</p>
<section class="card fine-agent"><div class="eyebrow">Creative Agent</div><h2>Fine Art Prompt Studio</h2><p class="muted">Plan the collection, choose customer sizes, then let the agent compile controlled art directions.</p><div class="fine-agent-grid"><form id="fine-agent-form"><label>Collection name <input name="collectionName" value="Women of Modern Life"></label><label>Artwork subject <textarea name="subject" rows="3">Museum-grade contemporary portrait, intimate direct gaze</textarea></label><label>Number of images to create <input name="imageCount" type="number" min="1" max="50" value="3"></label><label>Material & surface <select name="medium"><option value="heavy impasto oil texture, visible palette knife strokes on coarse canvas">Heavy canvas impasto</option><option value="fine grain watercolor on textured cotton rag paper">Classic watercolor grain</option><option value="classical fresco style with weathered plaster cracks">Antique fresco</option><option value="academic realism marble sculpture texture, raw chiseled details">Polished marble surface</option></select></label><label>Studio lighting <select name="mood"><option value="chiaroscuro lighting, deep dramatic shadows">Chiaroscuro</option><option value="soft museum gallery lighting, sfumato blending">Sfumato</option><option value="moody early 20th-century low-key illumination">Low-key mood</option></select></label><div class="recipe"><label class="range-line">Stylize <output id="stylize-value">300</output><input name="stylize" type="range" min="0" max="1000" step="50" value="300"></label><label class="range-line">Chaos <output id="chaos-value">10</output><input name="chaos" type="range" min="0" max="100" step="5" value="10"></label></div><label>Artwork shape <input name="aspectRatio" value="auto" type="hidden"><span>Free — follow the prompt</span></label><p id="creation-resolution" class="muted"></p><label class="mode"><input name="styleRaw" type="checkbox" checked> Use raw style</label><label class="mode"><input name="printTexture" type="checkbox" checked> Ask for tactile, print-real texture</label><label>Customer print sizes <span class="muted">Choose the sizes to offer · width × height</span></label><div class="mode-options">${PRINT_SIZES.map((size,index)=>`<label class="mode"><input name="deliverySize" value="${size.key}" type="checkbox" ${index<3?'checked':''}>${size.size}</label>`).join('')}</div><div class="actions"><button>Prepare art directions</button></div><p id="fine-agent-status" class="muted"></p></form><section><div class="eyebrow">Live creative brief</div><pre id="fine-agent-preview" class="prompt-preview"></pre><div id="delivery-plan" class="recipe"><div class="recipe-item"><b>UHD delivery</b>Choose print sizes to prepare the customer file plan.</div></div><p class="muted">Midjourney can be guided toward real material texture. Final 300 DPI UHD customer files are prepared after you select and upscale the artwork.</p></section></div></section>
<section class="card approval-studio"><div class="approval-head"><div><div class="eyebrow">Your approval room</div><h2 id="approval-title">Collection 01</h2><p>Tick artworks, choose formats, then approve or send them back for a variation.</p></div><button id="approve-all" class="secondary">Approve selected</button></div><div id="approvals" class="approval-grid">Loading artworks…</div></section>
<section class="card"><div class="approval-head"><div><div class="eyebrow">Autonomous creative control</div><h2>Your creative autopilot</h2><p>It plans and art-directs all 50 products in the background. You only step in to choose artwork and approve sales.</p></div><div class="actions"><button id="autopilot-start">Start autopilot</button><button id="autopilot-pause" class="secondary hidden">Pause</button></div></div><p id="autopilot-status" class="muted">Autopilot is waiting to begin.</p><div id="autopilot-next" class="recipe"></div><p id="auto-status" class="muted">No batch has been created yet.</p><div id="auto-prompts" class="auto-list"></div></section>
<section id="metrics" class="grid metrics"></section>
<section class="card"><h2>17-step production pipeline</h2><div id="stages" class="stage-list"></div></section>
<section class="two grid"><section class="card"><h2>Creative Prompt Agent</h2><p class="muted">Creates three distinct visual directions for a new idea.</p><form id="brief"><label>Artwork subject <input name="subject" required placeholder="e.g. celestial floral still life"></label><label>Goal <input name="goal" placeholder="e.g. premium printable wall art"></label><label>Audience <input name="audience" placeholder="e.g. contemporary interior collectors"></label><label>Mood <input name="mood" placeholder="e.g. quiet, luminous, editorial"></label><label>Palette <input name="palette" placeholder="e.g. midnight navy, gold and pearl"></label><label>Artwork shape <input name="aspectRatio" value="auto" type="hidden"><span>Free — follow the prompt</span></label><div class="actions"><button>Prepare 3 directions</button></div></form><p id="brief-status" role="status"></p><div id="candidates"></div></section>
<section class="card"><h2>First 50 digital products</h2><p class="muted">Your supplied catalogue. Prepare its original prompt as a Midjourney handoff, one product at a time.</p><div id="catalog" class="products">Loading catalogue…</div></section></section>
<section class="two grid"><section class="card"><h2>Waiting for selected Midjourney artwork</h2><p class="muted">Paste the final public Midjourney CDN image link after you choose the image.</p><div id="waiting"></div></section><section class="card"><h2>Learning insights</h2><div id="insights" class="muted">Loading…</div><h2 style="margin-top:24px">Recent work</h2><div id="recent" class="muted">No jobs yet.</div></section></section>
</main><script>
const $=s=>document.querySelector(s), el=(tag,props={},children=[])=>{const n=document.createElement(tag);Object.entries(props).forEach(([k,v])=>k==='class'?n.className=v:k==='text'?n.textContent=v:k==='checked'?n.checked=Boolean(v):k==='disabled'?n.disabled=Boolean(v):k.startsWith('on')?n.addEventListener(k.slice(2),v):n.setAttribute(k,v));children.flat().forEach(c=>n.append(c));return n};
const catalogIndex=new Map();
async function api(url,opts={}){const r=await fetch(url,{credentials:'same-origin',...opts,headers:{'content-type':'application/json',...(opts.headers||{})}});const data=await r.json();if(!r.ok)throw Error(data.error||'Request failed');return data}
const FLUX_MAX_PIXELS=${FLUX_MAX_PIXELS};const highestFluxSize=${highestFluxSize.toString()};const resolveArtworkRatio=${resolveArtworkRatio.toString()};const deliveryFormats=${JSON.stringify(PRINT_SIZES)};const fineAgentForm=$('#fine-agent-form');function fineAgentData(){const data=Object.fromEntries(new FormData(fineAgentForm));data.styleRaw=fineAgentForm.elements.styleRaw.checked;data.printTexture=fineAgentForm.elements.printTexture.checked;data.deliverySizes=[...fineAgentForm.querySelectorAll('[name=deliverySize]:checked')].map(input=>input.value);return data}function renderFineAgentPreview(){const data=fineAgentData();const batchInput=/^\s*prompt_id,batch_id,/i.test(data.subject)||[...data.subject.matchAll(/^\s*(?:\*\*)?[0-9]{1,3}\s*[—–-]\s+/gm)].length>1;fineAgentForm.elements.imageCount.readOnly=batchInput;$('#stylize-value').textContent=data.stylize;$('#chaos-value').textContent=data.chaos;$('#fine-agent-preview').textContent='Collection: '+data.collectionName+' · '+data.imageCount+' images\n\n'+data.subject+', '+data.medium+', '+data.mood+(data.printTexture?', tactile paint grain and authentic surface depth':'')+', gallery exhibition quality'+(data.styleRaw?' --style raw':'')+' --stylize '+data.stylize+' --chaos '+data.chaos;try{if(batchInput){$('#fine-agent-preview').textContent=data.subject;$('#creation-resolution').textContent='Batch detected · each artwork keeps its own prompt and ratio. One image per entry; the count is set when prepared.';}else{const ratio=resolveArtworkRatio(data.subject);const size=ratio?highestFluxSize(ratio):null;$('#creation-resolution').textContent=size?'From prompt · '+ratio+' · '+size.width+' × '+size.height+' px · PNG. Large prints still need preparation.':'Free shape — no fixed ratio. Add a ratio or square image in the prompt for FLUX.';}}catch(error){$('#creation-resolution').textContent=error.message;}renderDeliveryPlan({printSizes:deliveryFormats.filter(size=>data.deliverySizes.includes(size.key))});}function renderDeliveryPlan(delivery){const root=$('#delivery-plan');const sizes=delivery.printSizes||[];root.replaceChildren(...(sizes.length?sizes.map(size=>el('div',{class:'recipe-item'},[el('b',{text:size.size+' · 300 DPI'}),document.createTextNode(size.pixels+' · '+size.ratio)])):[el('div',{class:'recipe-item'},[el('b',{text:'UHD delivery'}),document.createTextNode('Select at least one customer size.')])]));}fineAgentForm.addEventListener('input',renderFineAgentPreview);fineAgentForm.addEventListener('change',renderFineAgentPreview);fineAgentForm.addEventListener('submit',async e=>{e.preventDefault();const status=$('#fine-agent-status'),data=fineAgentData();status.className='muted';status.textContent='Creative Agent is preparing directions…';try{const result=await api('/api/creative/prompts',{method:'POST',body:JSON.stringify({...data,goal:'premium collectible digital wall art',audience:'design-conscious interior collectors',palette:'graphite, pearl and deep charcoal'})});renderCandidates(result);renderDeliveryPlan(result.delivery);if(result.kind==='artwork-batch')fineAgentForm.elements.imageCount.value=result.artworks.length;status.className='ok';status.textContent=result.kind==='artwork-batch'?result.collectionName+' · '+result.artworks.length+' separate artworks prepared. Review each prompt and ratio below. No images generated.':result.delivery.collectionName+' · '+result.delivery.requestedImages+' images planned. Three directions are ready below.'}catch(error){status.className='error';status.textContent=error.message}});renderFineAgentPreview();
const n=v=>Number(v||0); function renderOverview(d){$('#mode').textContent=d.mode+' · '+(d.integrations.langgraph||'Creative approval workflow ready');const labels=[['awaiting_creation','Awaiting artwork'],['working','Processing'],['artwork_ready','Artwork ready'],['fulfilled','Delivered'],['failed','Needs review']];$('#metrics').replaceChildren(...labels.map(([k,l])=>el('div',{class:'card metric'},[el('b',{text:n(d.counts[k])}),el('small',{text:l})])));$('#stages').replaceChildren(...d.stages.map((s,i)=>el('div',{class:'stage'},[el('span',{class:'num',text:String(i+1).padStart(2,'0')}),el('div',{},[el('strong',{text:s.label}),el('p',{text:s.detail})]),el('span',{class:'state',text:s.state})])));renderWaiting(d.waitingJobs);const recent=d.recentJobs.length?d.recentJobs.map(j=>el('div',{class:'job'},[el('strong',{text:j.productTitle}),el('div',{class:'muted',text:j.status+' · '+j.id})])):[el('span',{text:'No jobs yet.'})];$('#recent').replaceChildren(...recent);}
function productModes(card){return [...card.querySelectorAll('[data-mode] input:checked')].map(input=>input.value)}function artworkLabels(card){return [...card.querySelectorAll('[data-artwork]:checked')].map(input=>input.value)}
async function saveApproval(card,status){const promptId=card.dataset.promptId;const state=card.querySelector('.approval-status');try{const board=await api('/api/approvals/'+encodeURIComponent(promptId),{method:'POST',body:JSON.stringify({status,productModes:productModes(card),selectedArtworkLabels:artworkLabels(card)})});renderApprovals(board)}catch(error){state.className='approval-status rejected';state.textContent=error.message}}
function renderApprovals(board){$('#approval-title').textContent=board.collection;const root=$('#approvals');root.replaceChildren(...board.artworks.map(item=>{const card=el('article',{class:'approval-card'});card.dataset.promptId=item.promptId;const images=el('div',{class:'approval-images'},item.artworks.map(image=>{const input=el('input',{type:'checkbox',value:image.label,'data-artwork':'',checked:item.approval.selectedArtworkLabels.includes(image.label)});return el('label',{class:'approval-image'},[el('img',{src:image.assetUrl,alt:item.title+' · '+image.label}),input])}));const modes=[['digital','Digital'],['print','Fine-art print'],['framed_print','Framed print']];const modeOptions=el('div',{class:'mode-options'},modes.map(([value,label])=>el('label',{class:'mode','data-mode':''},[el('input',{type:'checkbox',value,checked:item.approval.productModes.includes(value)}),document.createTextNode(label)])));const state=el('p',{class:'approval-status '+item.approval.status,text:item.approval.status==='pending'?'Waiting for your choice':item.approval.status==='approved'?'Approved for the collection':'Not going into this collection'});const approve=el('button',{text:'✓ Approve',onclick:()=>saveApproval(card,'approved')});const variation=el('button',{class:'secondary',text:'↻ Variation',onclick:()=>saveApproval(card,'pending')});const reject=el('button',{class:'secondary',text:'Reject',onclick:()=>saveApproval(card,'rejected')});card.append(images,el('div',{class:'approval-body'},[el('div',{class:'approval-tag',text:'Prompt '+item.promptId+' · '+item.suggestedPresentation}),el('h3',{text:item.title}),modeOptions,el('div',{class:'actions'},[approve,variation,reject]),state]));return card}));}
async function loadApprovals(){try{renderApprovals(await api('/api/approvals'))}catch(error){$('#approvals').textContent=error.message}}
function renderWaiting(jobs){const root=$('#waiting');root.replaceChildren(...(jobs.length?jobs.map(j=>{const link=el('a',{class:'button secondary',href:(j.handoff&&j.handoff.createUrl)||'https://www.midjourney.com/imagine',target:'_blank',rel:'noopener',text:'Open Midjourney'});const copy=el('button',{class:'secondary',text:'Copy prompt',onclick:async()=>{await navigator.clipboard.writeText(j.handoff?.prompt||'');copy.textContent='Copied'}});const input=el('input',{placeholder:'https://cdn.midjourney.com/...final.png',type:'url'});const status=el('p',{class:'muted'});const submit=el('button',{text:'Submit selected artwork',onclick:async()=>{try{const r=await api('/api/creation/jobs/'+encodeURIComponent(j.id)+'/result',{method:'POST',body:JSON.stringify({assetUrl:input.value})});status.className='ok';status.textContent='Artwork registered: '+r.job.status;loadOverview()}catch(e){status.className='error';status.textContent=e.message}}});return el('div',{class:'job'},[el('strong',{text:j.productTitle}),el('p',{class:'muted',text:j.handoff?.instructions||'Midjourney handoff'}),el('textarea',{readonly:'',value:j.handoff?.prompt||''}),el('div',{class:'actions'},[copy,link]),el('label',{text:'Selected artwork URL'},[input]),submit,status])}):[el('p',{class:'muted',text:'No artwork is waiting for selection.'})]));}
async function loadOverview(){try{renderOverview(await api('/api/overview'))}catch(e){$('#mode').textContent=e.message;$('#mode').className='error'}}
function renderCandidates(data){if(data.kind==='artwork-batch')return renderArtworkBatch(data);const root=$('#candidates');root.replaceChildren(...data.candidates.map((c,index)=>{const status=el('p',{class:'muted'});const copy=el('button',{class:'secondary',text:'Copy prompt',onclick:async()=>{await navigator.clipboard.writeText(c.handoff.prompt);copy.textContent='Copied'}});const pick=el('button',{text:'Choose this direction',onclick:async()=>{try{const r=await api('/api/creation/handoffs',{method:'POST',body:JSON.stringify({promptId:data.promptId,candidateIndex:index})});status.className='ok';status.textContent='Handoff ready. Copy it, create in Midjourney, then submit the selected image below.';loadOverview()}catch(e){status.className='error';status.textContent=e.message}}});return el('article',{class:'candidate'},[el('strong',{text:c.label||c.name}),el('p',{class:'muted',text:c.creativeRationale||c.rationale}),el('textarea',{readonly:'',value:c.handoff.prompt}),el('div',{class:'actions'},[copy,pick,el('a',{class:'button secondary',href:c.handoff.createUrl,target:'_blank',rel:'noopener',text:'Open Midjourney'})]),status])}));}
$('#brief').addEventListener('submit',async e=>{e.preventDefault();const st=$('#brief-status');st.className='muted';st.textContent='Creating directions…';try{const d=await api('/api/creative/prompts',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget))) });renderCandidates(d);st.className='ok';st.textContent=d.kind==='artwork-batch'?d.artworks.length+' separate artworks prepared. Review the batch below.':'Three directions are ready. '+(d.orchestration?.artDirectorNote||'Choose the one you love; nothing is created until you approve it.')}catch(err){st.className='error';st.textContent=err.message}});
function renderWaiting(jobs){const root=$('#waiting');root.replaceChildren(...(jobs.length?jobs.map(j=>{const item=catalogIndex.get(j.catalogId);const copy=el('button',{class:'secondary',text:'Copy final prompt',onclick:async()=>{await navigator.clipboard.writeText(j.handoff?.prompt||'');copy.textContent='Copied'}});const link=el('a',{class:'button secondary',href:j.handoff?.createUrl||'https://www.midjourney.com/imagine',target:'_blank',rel:'noopener',text:'Open Midjourney'});const input=el('input',{placeholder:'https://cdn.midjourney.com/...final.png',type:'url'});const status=el('p',{class:'muted'});const add=el('button',{text:'Add chosen image',onclick:async()=>{try{await api('/api/creation/jobs/'+encodeURIComponent(j.id)+'/result',{method:'POST',body:JSON.stringify({assetUrl:input.value})});status.className='ok';status.textContent='Artwork added.';loadOverview()}catch(e){status.className='error';status.textContent=e.message}}});const fields=item?[['Subject',item.subject],['Material',item.material],['Light',item.lighting],['Colours',item.palette],['Format',item.aspectRatio]]:[];const recipe=fields.length?el('div',{class:'recipe'},fields.map(([label,value])=>el('div',{class:'recipe-item'},[el('b',{text:label}),document.createTextNode(value||'—')]))):el('p',{class:'muted',text:'Your prompt is ready.'});return el('div',{class:'job'},[el('strong',{text:j.productTitle}),el('p',{class:'muted',text:'Your artwork recipe — no editing needed.'}),recipe,el('div',{class:'actions'},[copy,link]),el('label',{text:'Paste your chosen Midjourney image link here'},[input]),add,status])}):[el('p',{class:'muted',text:'No artwork is waiting for selection.'})]));}
async function loadCatalog(){try{const items=(await api('/api/catalog')).products;items.forEach(item=>catalogIndex.set(item.id,item));const root=$('#catalog');root.replaceChildren(...items.map(p=>{const st=el('span',{class:'muted'});const b=el('button',{class:'secondary',text:'Prepare handoff',onclick:async()=>{b.disabled=true;try{await api('/api/catalog/'+encodeURIComponent(p.id)+'/handoff',{method:'POST',body:'{}'});st.className='ok';st.textContent='Ready below';loadOverview()}catch(e){st.className='error';st.textContent=e.message}finally{b.disabled=false}}});return el('article',{class:'product'},[el('div',{class:'product-title',text:p.title}),el('div',{class:'product-meta',text:[p.category,p.aspectRatio].filter(Boolean).join(' · ')}),b,document.createTextNode(' '),st])}));}catch(e){$('#catalog').textContent=e.message}}
async function loadInsights(){try{const d=await api('/api/creative/insights');$('#insights').textContent=d.summary||'No feedback recorded yet.'}catch(e){$('#insights').textContent=e.message}}
function renderAutonomous(batch){const status=$('#auto-status');status.className=batch.promptCount?'ok':'muted';status.textContent=batch.promptCount?batch.promptCount+' prompts ready for your review. Copy one, then paste it into Midjourney when you are ready.':'No batch has been created yet.';const root=$('#auto-prompts');root.replaceChildren(...(batch.prompts||[]).map(item=>{const copy=el('button',{class:'secondary',text:'Copy prompt',onclick:async()=>{await navigator.clipboard.writeText(item.prompt);copy.textContent='Copied'}});const open=el('a',{class:'button secondary',href:'https://www.midjourney.com/imagine',target:'_blank',rel:'noopener',text:'Open Midjourney'});return el('div',{class:'auto-item'},[el('span',{text:item.id+'. '+item.title}),el('div',{class:'actions'},[copy,open])])}));}
function renderAutopilot(data){const status=$('#autopilot-status'),start=$('#autopilot-start'),pause=$('#autopilot-pause'),next=$('#autopilot-next');const active=data.status==='active';start.classList.toggle('hidden',active);pause.classList.toggle('hidden',!active);status.className=active?'ok':'muted';status.textContent=active?'Autopilot is active · '+data.planned+' product plans are ready.':'Autopilot is '+data.status+'.';next.replaceChildren(...(data.next?[el('div',{class:'recipe-item'},[el('b',{text:'Next creative decision'}),document.createTextNode(data.next.title)]),el('div',{class:'recipe-item'},[el('b',{text:'System rule'}),document.createTextNode('Nothing is published or sent without you.')])]:[el('div',{class:'recipe-item'},[el('b',{text:'Ready when you are'}),document.createTextNode('Start autopilot to create the 50-product plan.')])]));}
async function loadAutonomous(){try{renderAutonomous(await api('/api/creative/autonomous'))}catch(e){$('#auto-status').textContent=e.message}}
async function loadAutopilot(){try{renderAutopilot(await api('/api/autopilot'))}catch(e){$('#autopilot-status').textContent=e.message}}
$('#autopilot-start').addEventListener('click',async()=>{const button=$('#autopilot-start');button.disabled=true;$('#autopilot-status').textContent='Autopilot is planning the collection…';try{renderAutopilot(await api('/api/autopilot/start',{method:'POST',body:'{}'}));await loadAutonomous()}catch(e){$('#autopilot-status').className='error';$('#autopilot-status').textContent=e.message}finally{button.disabled=false}});
$('#autopilot-pause').addEventListener('click',async()=>{try{renderAutopilot(await api('/api/autopilot/pause',{method:'POST',body:'{}'}))}catch(e){$('#autopilot-status').className='error';$('#autopilot-status').textContent=e.message}});
loadOverview();loadCatalog();loadInsights();loadAutonomous();setTimeout(loadOverview,500);setInterval(loadOverview,10000);
loadAutopilot();setInterval(loadAutopilot,10000);
$('#approve-all').addEventListener('click',async()=>{const cards=[...document.querySelectorAll('.approval-card')];for(const card of cards)if(artworkLabels(card).length)await saveApproval(card,'approved')});loadApprovals();
</script></body></html>`;
import { PRINT_SIZES } from './print-sizes.js';
import { CREATION_FORMATS, FLUX_MAX_PIXELS, highestFluxSize } from './creation-formats.js';
import { resolveArtworkRatio } from './prompt-shape.js';
